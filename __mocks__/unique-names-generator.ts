export const uniqueNamesGenerator = jest.fn((config: any) => {
    return 'MockedName';
});

export const starWars = {
    adjectives: ['mocked-adjective'],
    colors: ['mocked-color'],
    animals: ['mocked-animal'],
};
